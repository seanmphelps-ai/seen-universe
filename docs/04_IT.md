# DOCUMENT 4 — IT

Name: It.
Status: architecture memory. Not first slice. Grok Bot does not build this yet.

## What It is

Portals are I Ching bones with Human Design depth. SEEN adds lived years, dark time pick, and soil. That is the extra support those two systems do not have.

## The rule — no bypass

A native marker keeps its full weight. A second system does not replace it. It adds resolution only where there is a real mapping.

```
WESTERN FINDS
Pluto conjunct Ascendant
        → keep full weight
DEGREE / POSITION
        → Gate / Hexagram at that degree
COMPOSITE
Pluto × Ascendant × Gate × Hexagram
        × lived place and years already collected
SEEN ASKS
pressure / trigger / adaptation / behavior /
payoff / cost / consequence / people / place /
time / recurrence / failure / potential
        → file into a Life Section when that list exists
        → keep the source tag on every deposit
```

Pluto conjunct Ascendant does not become Gate 28.
Gate 28 does not become Pluto.

"Every planet/house gets run through its gate. No bypass."

An old reading line like "Gate 28 + Pluto Asc = soul-death ritual" is not a calculation rule. Different seed, place, year, and people fire differently. Swiss computes. Location and years shape the sentence. They do not rewrite the sky.

## Three operations later — not now

1. DISCOVERY — each modality finds only what it knows. Separate workers. No shared memory mid-run.
2. RESOLUTION — cross only where a real mapping exists (degree → gate). Native weight stays.
3. DISSECTION — ask the pressure lattice of the higher-resolution signal. File. Keep source.

No mash. No rewrite of the original marker.

## First slice still wins

`docs/00_GROK_BOT_FIRST_SLICE.md` is the only build file right now.
It does not run gates, hexagrams, 45 sections, ripples, or person-vs-person.

When two dark cards work, next files in order:
1. `docs/45_LIFE_SECTIONS.md` — 45 named ids
2. `docs/64_PORTALS.md` — 64 names + the question each asks
3. Then It can run on top of those lists
