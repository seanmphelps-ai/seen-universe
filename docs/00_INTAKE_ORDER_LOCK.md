# INTAKE ORDER LOCK — 2026-09-17

One path. Two old orders are a bug. This file beats foundation §2.0–2.6 and beats any "environment before birth date" line.

## What the first product is

A dark chart. How the system collapses under pressure. Not a pretty natal printout.

## Order

1. Name of the system being read (self, ex, parent, child)
2. Birth date
3. Birth city
4. Time rectification — hidden Western 04:00 / 12:00 / 20:00 — three pressure cards, no clock — pick — then ±3h / ±2h / ±1h
5. Lived locations 6+ months — three summaries per place, gift AND cost on every card — pick
6. Family as soil — modifiers on the same 45 life sections, not a separate religion
7. Then portals / helix / cadence

## Forces

- Seed = natal (date + locked time)
- Soil = family
- Weather = city / years lived
- Equal forces. No shout. No whisper. Sensitivity is discovered by which card they pick, not pre-weighted.

## Forbidden

- Location field before birth date
- "Place is primary" as an intake-order rule
- Optional time field
- Noon as a stand-in
- Clock on recognition cards
- Only-good or only-bad location cards
- Environment scoring that rewrites the seed before the seed is calculated

See `docs/TIME_NARROWING_LOCK.md` for the ladder.
See `docs/contexts/CLAUDE_LOCATION.md` for gift+cost cards.
